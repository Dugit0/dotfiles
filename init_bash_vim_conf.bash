# #!/bin/bash
touch "$HOME/.vimrc"
echo 'source ~/.config/vim/vimrc' > "$HOME/.vimrc"
# cat "$HOME/.vimrc"
touch "$HOME/.bashrc"
echo 'source ~/.config/bash/bashrc' > "$HOME/.bashrc"
# cat "$HOME/.bashrc"
